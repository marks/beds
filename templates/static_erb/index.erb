<div class="well">
  <div class="hero-unit">
    <h1>Models</h1>
    <p class="btn-group">
      <% @models.each do |model| %>
        <a class="btn btn-large btn-info" href="/<%=model.name.downcase%>/"><%= model.name %></a>
      <% end %>
    </p>
  </div>
</div>
